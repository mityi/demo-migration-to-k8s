spring:
  application:
    name: greeting-server

server:
  port: ${PORT:0}

eureka:
  instance:
    instance-id: ${spring.application.name}-${random.int}

management:
  endpoints:
    enabled-by-default: false
  endpoint:
    info:
      enabled: true
    health:
      enabled: true

